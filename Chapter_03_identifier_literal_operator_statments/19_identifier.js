//let class = "Hello";
//Class is a reserved word 

let retrun = 5;
// return is a reserved word

let myvariable = 10;
// myvariable is a valid identifier

let undefined = 20;//never used 
// undefined is a reserved word, but it can be used as an identifier (not recommended)
let infiinity = 30;// never used
// infinity is a reserved word, but // it can be used as an identifier (not recommended)
//let if =true;
// if is a reserved word but it can be used as an identifier (not recommended)

